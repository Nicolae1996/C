/*
	Probleme 4
	Determiner la moyenne des elements situe entre min et max du tableau		
*/
#include<iostream>

using namespace std;

main(){
	/*
	int a[] = {4 , 9, 2, 1, 6};
	int somme = 0;
	int count = 0;
	int max = a[0];
	int min = a[0];
	
	int pozmin, pozmax;
	for (int i = 1; i<5;i++)	{
		if (a[i]>max) 
		{
			max = a[i];	
			pozmax = i;
		}
		if (a[i]<min) 
		{
			min = a[i];
			pozmin = i;
		}
	}
	if (pozmin < pozmax)
	{
	for (int j = pozmin + 1; j<= pozmax - 1; j++)
	{
		somme +=a[j];
		count++;
	}
	}
	else {
	for (int j = pozmin -1; j >= pozmax + 1; j--)
	{
		somme +=a[j];
		count++;
	}
	}
if (somme == 0)
{
	cout<<"Media: "<<"0";
}
else
{
	cout<<"Media: "<<(somme/count);
}
*/

/*
	Tableaux unidimensionelle
	Probleme 5
	On considere un tableau bidimensionelle, n <= 20, m <= 30
	Determinner le nombre des elements impares de chaque ligne, 
	Les sommes des elements des colonnes paires et le produit des elements 
	situe sur la diagonale principale et secondaire.
*/
}
